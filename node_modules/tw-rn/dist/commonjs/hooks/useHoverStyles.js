"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useHoverStyles = void 0;

var _react = require("react");

const useHoverStyles = (styles, onMouseEnter, onMouseLeave) => {
  const [isHovered, setIsHovered] = (0, _react.useState)(false);
  const hoverStyles = (0, _react.useMemo)(() => {
    return styles.map(style => {
      if (style === undefined) return;
      return isHovered ? style.hover || {} : {};
    });
  }, [styles, isHovered]);
  const hasHoverStyles = (0, _react.useMemo)(() => {
    return styles.length > 0 && styles.some(style => style && style.hover && Object.keys(style.hover).length > 0);
  }, [styles]);
  const handleOnMouseEnter = (0, _react.useCallback)(e => {
    onMouseEnter === null || onMouseEnter === void 0 ? void 0 : onMouseEnter(e);
    if (hasHoverStyles) setIsHovered(true);
  }, [hasHoverStyles, hoverStyles, onMouseEnter]);
  const handleOnMouseLeave = (0, _react.useCallback)(e => {
    onMouseLeave === null || onMouseLeave === void 0 ? void 0 : onMouseLeave(e);
    if (hasHoverStyles) setIsHovered(false);
  }, [hasHoverStyles, hoverStyles, onMouseLeave]);
  return {
    hoverStyles,
    handleOnMouseEnter,
    handleOnMouseLeave
  };
};

exports.useHoverStyles = useHoverStyles;
//# sourceMappingURL=useHoverStyles.js.map