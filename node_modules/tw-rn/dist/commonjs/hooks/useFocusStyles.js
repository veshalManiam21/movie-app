"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useFocusStyles = void 0;

var _react = require("react");

const useFocusStyles = (styles, onFocus, onBlur) => {
  const [isFocused, setIsFocused] = (0, _react.useState)(false);
  const focusStyles = (0, _react.useMemo)(() => {
    return styles.map(style => {
      if (style === undefined) return;
      return isFocused ? style.focus || {} : {};
    });
  }, [styles, isFocused]);
  const hasfocusStyles = (0, _react.useMemo)(() => {
    return styles.length > 0 && styles.some(style => style && style.focus && Object.keys(style.focus).length > 0);
  }, [styles]);
  const handleOnFocus = (0, _react.useCallback)(e => {
    onFocus === null || onFocus === void 0 ? void 0 : onFocus(e);
    if (hasfocusStyles) setIsFocused(true);
  }, [hasfocusStyles, focusStyles, onFocus]);
  const handleOnBlur = (0, _react.useCallback)(e => {
    onBlur === null || onBlur === void 0 ? void 0 : onBlur(e);
    if (hasfocusStyles) setIsFocused(false);
  }, [hasfocusStyles, focusStyles, onBlur]);
  return {
    focusStyles,
    handleOnFocus,
    handleOnBlur
  };
};

exports.useFocusStyles = useFocusStyles;
//# sourceMappingURL=useFocusStyles.js.map