import { useMemo } from "react";
import { getStylesFromPlatform } from "../helpers";
export const usePlatformStyles = styles => {
  return useMemo(() => {
    return styles.map(style => getStylesFromPlatform(style));
  }, [styles]);
};
//# sourceMappingURL=usePlatformStyles.js.map